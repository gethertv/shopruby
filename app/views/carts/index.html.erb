<p style="color: green"><%= notice %></p>

<h1>Koszyk</h1> <br />

<%= button_to "New cart", create_new_cart_path, class: 'btn-add', method: :post %> <br />

<div id="categories">
  <table id="customers">
    <thead>
    <tr>
      <th>Nazwa</th>
    </tr>
    </thead>
    <tfoot>


  <% @carts.each do |cart| %>
    <%= render cart %>
      <tr>
        <td> <%= link_to "Zobacz koszyk ##{cart.id}", cart %></td>
      </tr>
  <% end %>

    </tfoot>
  </table>
</div>


